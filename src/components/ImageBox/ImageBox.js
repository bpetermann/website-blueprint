import React from 'react';
import styles from './ImageBox.module.css';

const ImageBox = (props) => {
  return (
    <div className={styles['container']}>
      {/* <img
        src={require('../../images/landscape.png')}
        alt='Landscape'
        className={styles['image']}
      /> */}
    </div>
  );
};

export default ImageBox;
